# Dependencies (covers both frontend and backend if node_modules exists there)
**/node_modules/
.pnp
.pnp.*

# Environment Variables (covers root, backend, etc.)
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

# OS generated files
.DS_Store
Thumbs.db

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.cursor-history/
.cursorrules

# Build outputs (add backend specific if any, e.g., /dist, /build)
# Example: backend/dist

# Optional: If you have specific files in the root to ignore
# my-root-config.local

# Exclude PDF files
*.pdf

# Exclude Text files (Use with caution - may ignore needed files)
*.txt
# Example exception: !requirements.txt
